import RegisterForm from '@/components/Register/Register';
import LogoutForm from '../../components/Login/Login';
import { Box } from '@mui/material';

const RegisterPage = () => {
  return (
    <Box>
      <RegisterForm />
    </Box>
  );
};

export default RegisterPage;